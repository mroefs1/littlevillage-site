# littlevillage-site

Replacement website for littlevillage.org (The Hagedorn Little Village School), moving off WordPress. Static marketing/content site: programs, staff, news, events, contact.

**Status:** Steps 1-8, 9 (News & Events detail pages), 10 (Parent Association), and 11 (Visual Restyle) are complete and live on the Cloudflare Pages preview. Only Step 9d (custom domain cutover, deferred to launch week) remains. Full history of that work - including corrections and decisions made along the way - is archived in `docs/archive/completed-batches.md`. This file covers active/upcoming work only.

**Launch target: mid-September 2026, at an in-person event.** Until then, all work targets the `littlevillage-site.pages.dev` preview only. Custom domain cutover and DNS changes are explicitly out of scope until launch week - do not touch DNS or add the custom domain to the Cloudflare Pages project before then, even if asked to "finish" the deploy pipeline.

**Full plan and rationale:** see the "Little Village Site Rebuild - Jaspr + Sanity" Notion page.

## Tech stack (fixed, don't deviate without asking)

- **Jaspr** - Dart web framework, `mode: static` (SSG). Not an SPA, not SSR, for now.
- **Styling:** Jaspr's native type-safe CSS-in-Dart (`css()`/`@css`/`Styles`). No CSS framework, no preprocessor.
- **Sanity** - CMS, source of truth for all content. Project `f537tj40`, dataset `production`. Same project/dataset as the existing Dart app. Schema lives in the separate `hlvs-studio` repo (`hlvs/schemaTypes/`).
- **Cloudflare Pages** - static host. Preview build at `littlevillage-site.pages.dev` is the only target until launch.
- **Serverpod** - not yet. Future addition once genuinely dynamic/authenticated features are needed (donation flow, parent/staff portal). Contact form backend will NOT use Serverpod - see Step 9c below.

## Guiding principles

- **Dart-first, minimal JS.** Avoid hand-written JS. Exceptions: Sanity client/tooling JS, and Cloudflare Pages Functions (which don't run Dart - see Step 9c).
- **Content boundary:** Sanity owns all editable content. Jaspr owns layout/logic only.
- **Data layer:** never call the Sanity client directly from page components - always go through `ContentRepository` (`lib/sanity/`). This is what lets Serverpod slot in later without a content-layer rewrite.
- **Work in batches, one screen/task at a time.** Finish, verify, and commit one batch before starting the next.
- **Nav is a hardcoded array in `header.dart`.** `SiteSettings.navigation` is queried but intentionally unused - follow the existing pattern, don't switch to Sanity-driven nav without discussing it first.

## Known area of concern: Dart SDK pinning in `build.sh`

`build.sh` fetches the Dart SDK itself (Cloudflare's build image doesn't ship Dart) and used to always grab `channels/stable/release/latest`. **On 2026-08-12, Dart 3.13.0 broke the Cloudflare build** - `jaspr build` crashed and then hung (not a clean failure) partway through static route generation, on `/history`, with a `NoSuchMethodError: The method '&' was called on null` several layers deep inside `dart:_compact_hash`, inside Jaspr's async SSR rebuild machinery (`AsyncBuildOwner`/`TaskChain`) - not application code. The exact same commit built cleanly, repeatedly, all session on a local machine running Dart 3.12.1. Fix: `build.sh` now pins `DART_SDK_VERSION="3.12.1"` explicitly instead of `latest`. **If a future Cloudflare build fails or hangs with no code-level explanation, check this first** - it may mean Dart shipped a new stable release that regressed something Jaspr depends on, same failure mode as this incident. Bump the pin deliberately (test locally on the new version first) rather than silently reverting to `latest`.

## Complete: Step 9b - Sanity Rebuild Webhook

Verified working 2026-08-10. Publishing/updating/deleting content in Sanity Studio triggers a Cloudflare Pages rebuild of `littlevillage-site.pages.dev` automatically. Dev preview only - no domain/DNS involved. See `docs/archive/completed-batches.md` for details if needed.

## Complete: Step 9c - Contact Form Backend

Verified working 2026-08-11. The Contact page's "Request Information" and "Schedule a Tour" flows collapsed into one form with a mandatory General Inquiry / Schedule a Tour dropdown (no default selection), honeypot + Cloudflare Turnstile bot protection, and a Cloudflare Pages Function (`functions/api/contact.js`) that sends via Resend from `send.littlevillage.org` to `information@littlevillage.org`. The `ContactForm` `@client` component (`lib/components/contact_form.dart`) handles the real submit flow, including loading/success/error states, required-field indicators (asterisk + screen-reader text, not color-only), and focus/`aria-invalid` handling on the dropdown if the disabled-submit-button UX is ever bypassed. See `docs/archive/completed-batches.md` for details if needed.

## Complete: Step 11 - Visual Restyle (Pastel/Fraunces)

All 10/10 batches complete and verified 2026-08-12. Management requested a full-site restyle: current fonts felt "childish," background felt bland. Design produced via Claude Design - handoff folder with 3 reference pages (home/admissions/contact HTML+screenshots) plus a README with the full token system. **This was a skin change only** - no layout, IA, or copy changes. Same components, same structure, same content, just restyled.

**Full token replacement** - old color/font tokens in `theme.dart` were fully replaced, not kept alongside the new ones - confirmed at 11.10 via a full-codebase grep finding zero remaining legacy token references.

**Note on the coral accent:** the design tokens table below still shows the handoff's literal `#ef7d68` for historical reference, but the actual token in `theme.dart` is `#ce3216` - darkened during 11.10's WCAG AA pass after the literal handoff color failed contrast badly (2.4-2.7:1, needs 4.5:1) as both text-on-white and white-button-text-on-coral. See the 11.10 entry below for the full reasoning.

**Sequencing constraint satisfied:** Step 11 was fully complete and verified before Step 9d (custom domain cutover) - 9d remains deferred to launch week regardless, per its own section below.

**Global rule for every batch below - style-only:**
No changes to component structure/props, state logic, event handlers, GROQ queries, `ContentRepository`, `functions/api/contact.js`, or ARIA attributes/roles (presence stays identical - only visual treatment changes). No changes to interactive behavior: disabled-button gating on the contact form, the honeypot's visually-hidden-not-display-none technique, Turnstile flow, FAQ expand/collapse, hero carousel autoplay/pause/arrows, mobile nav toggle. If a CSS change touches an element tied to interactive behavior, verify that behavior still works before marking the batch done - a margin or color change silently breaking a hover/focus region is exactly the failure mode to catch here.

**Design tokens (from handoff README):**

- Fonts: Fraunces (serif, headings, weight 500-700, tight letter-spacing -0.015em to -0.02em on H1/H2) + DM Sans (body/UI, weight 400-700). Google Fonts: `family=DM+Sans:wght@400;500;600;700&family=Fraunces:opsz,wght@9..144,500;9..144,600;9..144,700`
- Colors: navy ink `#17334a` (text), navy-dark `#143c55`/`#102f43` (header bars, footer), blue `#356e8d` (nav links), sky `#eaf5f8`, cream `#fffaf2`, coral `#ef7d68` (the ONLY saturated accent - primary buttons/links/active states), peach `#fff0e8`/`#fde3d5`, green `#4e8b75` (check icons), mint `#e7f3ed`/`#d9ecdf`, yellow `#f5c75f` (step-number circles, small highlights), line/border `#dce7eb`/`#cbdbe2`, muted text `#4e6879`/`#546b78`/`#728591`. Max 2 background tones per page plus the coral accent.
- Shape: card radius 20-28px, pill buttons `border-radius: 999px`, section vertical padding ~56-72px.

- [x] **11.1 - Design tokens foundation** - `theme.dart` fully replaced: new `AppColors` palette (canonical names - navy/navyDark/navyDarker/blue/sky/cream/coral/peach/peachDark/green/mint/mintDark/yellow/line/lineDark/mutedText\*), Fraunces + DM Sans fonts (Google Fonts import updated), new `Radii` and `Spacing` token classes (additive, not yet consumed anywhere). **Legacy token names kept as aliases** (e.g. `AppColors.primary = blue`, `AppColors.accent = coral`) pointing at the new values, so all ~200 existing call sites across 17 files compile and pick up the new colors without any component file being touched this batch - migrate call sites to the canonical names as each page's batch touches that component, then drop the alias. Verified: `dart analyze` clean project-wide, static build succeeds for all 21 routes, and a real headless-browser render confirms both Fraunces and DM Sans actually load and paint (not just fall back), plus the new coral/navy/sky colors rendering on Home and Admissions. **Blast radius flagged for later batches:** two hardcoded (non-token) hex colors exist outside `theme.dart` - `#b3261e` in `lib/components/contact_form.dart` (error-state red) and `#fbfaf8` in `lib/pages/contact.dart` (background) - both belong to 11.5. Separately, `FontFamilies.cursive` is hardcoded as the generic CSS fallback alongside `headingFontFamily` at 81 call sites across 19 files (not just in `theme.dart`, which now correctly uses `FontFamilies.serif`) - cosmetic only (matters solely if Fraunces fails to load), but each page batch should fix its own occurrences when it touches that component.

- [x] **11.2 - Header, utility bar, footer (global chrome)** - `header.dart`, `footer.dart`, `mobile_nav.dart` migrated to the canonical token names (navy/navyDark/navyDarker/blue/coral/yellow/sky/line/mutedTextLight - no more legacy aliases in these three files). Utility bar flipped to a dark navy bar with white text and a coral pill Donate button (was a light bg before, per the reference); logo gets a yellow circular badge treatment via CSS on the existing `<img>` (no new markup); nav links recolored blue with DM Sans (was navy/Fraunces-ish, matching the reference's "playful" font only being reserved for real headings now); Request Info button switched from blue to coral to match its CTA role; footer bg → navy-darker, column titles → yellow uppercase DM Sans labels (were plain white, no transform). Donate-pill focus outline uses white (not blue) against the new dark bg, matching the existing documented rationale already used in the footer. Fixed the `FontFamilies.cursive` generic-fallback mismatch (now `.serif`) in these three files' remaining Fraunces usages, and re-tinted two dropshadow `rgba()` values that were still keyed to the old ink color. **Verified:** `dart analyze` clean, static build succeeds, and a headless-browser pass confirms both interactive behaviors still work post-restyle - the Programs dropdown opens via real `:hover` and via keyboard `:focus-within`, and the mobile hamburger's `aria-expanded` toggle and nav visibility both flip correctly on click.

- [x] **11.3 - Homepage** - `home.dart` fully restyled to the canonical tokens (no legacy aliases left), plus a handful of value-only fixes derived from reading the actual reference markup closely: trust strip changed from one shared-border box to individual left-accent cards (matches reference), age-locator cards get per-card pastel backgrounds via `:nth-child` (peach/sky/mint, no markup change) while **keeping real Sanity photos** rather than adopting the reference's flat placeholder-only card look - the handoff README explicitly says the mockup's placeholders stand in for real photography, so real images are the correct call here, not a deviation. Enrollment teaser lost its bordered/shaded box (reference uses a plain section), donate band went full-bleed yellow with navy-dark text/button (not the coral/peach treatment the legacy `warm*` aliases had implied), news/event rows got a coral Fraunces date badge, current-families icon tiles get distinct peach vs. mint striping via `nth-child` (no modifier class needed). `hero_gallery.dart` radius bumped to 32px and its one `AppColors.ink` reference migrated to `navy`. **Verified:** `dart analyze` clean, static build succeeds, full-page screenshots at desktop and mobile widths match the reference direction, and - since this page carries the highest interactive risk in Step 11 - a scripted headless-browser pass confirmed every hero carousel behavior still works: autoplay advances (~4s interval), hovering pauses it, un-hovering resumes it, both manual arrows work, and `prefers-reduced-motion: reduce` disables autoplay while manual arrows keep working.

- [x] **11.4 - Admissions** - `admissions.dart` and `faq_accordion.dart` fully migrated to canonical tokens. Also restyled the shared `.page-breadcrumb`/`h1`/`h2` rules in `content_page.dart` (used by ~17 pages) since Admissions is the first page batch to touch that shared wrapper - left `.detail-*`/`.link-*` in that same file alone since those belong to News/Events detail (11.8) and the About hub (11.7). Eligibility checklist became one peach card (was a bordered box with a separate colored header bar); enrollment journey converted from a vertical list of photo-rows to a 2x2 grid of cards - kept real Sanity photos rather than the reference's photo-less 4-column layout, same reasoning as 11.3's age cards. $0 callout is now a navy-dark band with a yellow Fraunces value (was a light sky/blue box). Closing CTA flipped from a solid-primary-color box to a light sky section with three pill buttons (was a dark box with white text). The eligibility checkmarks stayed as literal `✓ Label` text (not split into a separate icon element) since the checkmark character is existing page copy, not decoration - same restraint applied to the footer's "↳" arrows in 11.2. **Verified:** `dart analyze` clean, static build succeeds, full-page screenshot matches the reference direction, and a scripted headless-browser pass confirmed the FAQ accordion's actual behavior is untouched - single-open-at-a-time toggling, `aria-expanded` flips correctly per item, and the answer's conditionally-rendered DOM node still tracks open state exactly as the original (unmodified) `_openIndex` state logic dictates.

- [x] **11.5 - Contact page (handle carefully - newest, most fragile code)** - `contact.dart` and `contact_form.dart` migrated to canonical tokens; `_dangerColor` (`#b3261e`, the required-mark/error color) deliberately left as its own standalone constant, not folded into the brand palette, since it's a semantic error color with no equivalent in the design tokens. Also cleared the `#fbfaf8` hardcoded hex flagged back in 11.1 - inputs are white per the reference, not that off-white. Discovered `#f7fafb` recurs in two independent reference files (home.html's news section, contact.html's form card) despite not being in the README's summary table, so added it as a new canonical `AppColors.offWhite` token rather than hardcoding it again - used for the form card background. Info card dropped its bordered-box treatment for plain gap-stacked rows (reference has no border there); map placeholder gets a page-scoped sky-striped override (`.contact-info-column .photo-placeholder`) instead of changing the shared `photo_placeholder.dart` component's default peach stripes used elsewhere. **Verified with a real functional pass, not just a visual check:** scripted headless-browser runs confirmed the submit button is disabled until a request type is chosen, the honeypot's computed style is still `position:absolute; left:-9999px` with `display:block` (never `display:none`) and `tabindex="-1"`, the client-side validation cascade fires in the correct order (name → message → phone-or-email), and - using route interception to control the `/api/contact` response - all three request states render correctly: loading (button disabled, `aria-busy="true"`, spinner visible, "Sending…"), a mocked failure (red `role="alert"` error text, button re-enabled), and a mocked success (confirmation message, `role="status"`).

- [x] **11.6 - Programs hub + 3 detail pages** - no reference HTML exists for this page (the handoff only covers home/admissions/contact), so this batch applied the design language already established in 11.1-11.5 consistently rather than pixel-matching a mockup. `programs.dart`, `program_detail.dart`, `cta_band.dart`, and `pill_list.dart` all migrated to canonical tokens. `cta_band.dart` is the actual shared component (unlike Admissions, which turned out to have its own local duplicate of this same "Ready to take the first step?" band) - restyled to match the identical sky/pill treatment already built for Admissions' local copy, so both read as the same component across the site. Programs hub cards reuse the exact peach/sky/mint per-card pattern from the homepage age-locator cards, since they're literally the same three age-band programs. Program detail pages: yellow pill age-badge, line-bordered timeline cards with coral time labels, sky "how to start" teaser - its CTA link deliberately kept as plain coral text rather than a pill button, matching the site-wide convention that pills are for primary navigation actions while in-card CTAs stay as text links. **Verified:** `dart analyze` clean, static build succeeds, and screenshots of the hub plus all 3 detail pages (Early Intervention, Preschool, Elementary) confirm the result reads as one consistent system with the rest of the site.

- [x] **11.7 - About family (About/Mission/History/Founders/Staff/Board) + Facilities** - `about.dart` migrated to canonical tokens (sub-nav links go blue, stat strip becomes individual yellow-accent cards matching the homepage trust-strip pattern, mission photo/team cards recolored, accreditation strip becomes a mint card). Mission/History/Founders/Facilities have no page-specific CSS at all - they're pure `ContentPage` + `PortableTextView`, so restyling `portable_text_view.dart`'s `.body-content` (mutedText body copy, navy headings) was enough to cover all four at once. Staff/Board share `collection_card.dart` (`.card-grid`/`.collection-card`) - also used by News/Events (11.8) and Parent Association (11.9), so - same precedent as `cta_band.dart` in 11.6 - restyled it now since this is the first batch to touch it (line border, navy Fraunces title, coral CTA, uppercase muted eyebrow). Corrected a stale mobile-breakpoint override on `.about-stat` left over from the old shared-divider design, same fix as the trust-strip in 11.3. **Verified:** `dart analyze` clean, static build succeeds, screenshots of About/Mission/Facilities confirm real Sanity content (bullet lists, multiple headings) renders correctly with the new typography. Staff/Board showed only the "coming soon" empty-state copy in this dev dataset (no seeded staff/board content to render `CollectionCard` against) - the code change is the same direct CSS pattern already visually verified on `.prog-card`/`.age-card` elsewhere, so this is a lower-confidence verification than the rest of the batch and worth a spot-check once real staff/board content exists in Sanity.

- [x] **11.8 - News/Events listing + detail pages + newsletter links** - closes out `content_page.dart`'s `.detail-*`/`.link-*` rules deferred since 11.4 (confirmed `.link-grid` is newsletter cards, not an About hub - About actually dropped that pattern entirely, per its own code comment). `news_events_filter.dart`'s pills go line-bordered/navy with a coral active state (was blue); `event_detail.dart`'s own gallery heading/grid migrated to canonical tokens. Gave the News & Events intro paragraph a `mutedText` subtitle treatment matching every sibling page (Admissions/Programs/About/Contact) - it previously had no class or styling at all, which would have read as a visible inconsistency against the rest of the now-restyled site; this was the one non-trivial addition this batch (a new CSS class + a small `@css` block on a page that previously had none), everything else was value-only changes to existing rules. **Verified:** `dart analyze` clean, static build succeeds, and a scripted headless-browser pass confirmed the filter pills' actual behavior is untouched - `aria-pressed` flips correctly across all three pills and the News/Events card grids show and hide exactly as expected when filtering, not just a visual glance at the colors.

- [x] **11.9 - Current Families + Parent Association** - `current_families.dart` and `parent_association.dart` migrated to canonical tokens. Feature cards and PA dues cards get the same peach/mint per-item differentiation used elsewhere (Current Families feature icons via `nth-child`, matching the homepage's 11.3 pattern); boxed/bordered quicklinks and calendar-embed wrapper simplified along the same lines as prior batches' box-to-plain-section changes. Inherits the shared `.card-grid`/`CollectionCard` (11.7) and `.detail-cta` (11.8) styling automatically for PA events and the membership signup button. **Verified: all three items this batch calls out.** The Google Calendar iframe embed renders with its real `src`/`title` intact and shows live calendar data at full size - confirmed via headless browser, not just a visual glance. The PA events empty-state text and its link to `/current-families#calendar` are byte-for-byte unchanged. The Current Families nav dropdown opens correctly on both hover and keyboard focus - initial automated check falsely reported it broken because the test script queried `document.querySelector('.nav-dropdown-menu')` unscoped, which silently grabbed the _first_ of three dropdowns on the page (Programs) instead of the one actually being hovered; properly scoped, the dropdown was never actually broken. Screenshots of both pages confirm real Sanity content (PA board members, contacts, dues) renders correctly.

- [x] **11.10 - 404 page + final full-site sweep** - `not_found.dart` restyled to canonical tokens.

  **Real WCAG AA contrast pass (computed, not eyeballed)** - checked every color pairing actually used across all of Step 11 against its real background(s). Found genuine, non-borderline failures: the handoff's literal `coral` (`#ef7d68`) cleared only 2.4-2.7:1 as text-on-white/pastel-card and as the background under white button text (needs 4.5:1) - not an edge case, this covers effectively every CTA button and coral text link sitewide. `mutedTextLight` (`#728591`) cleared only 3.4-3.8:1 against its real backgrounds, short of the 4.5:1 normal-text floor. Confirmed with the user before acting, then darkened both tokens in `theme.dart`: `coral` → `#ce3216` (5.0-5.1:1 against every real background it's used on; hue essentially unchanged at 9.3°→9.4°, so it's a lightness/saturation darken, not a new color, and stays visually distinct from the unrelated form-error red in `contact_form.dart`), `mutedTextLight` → `#5f707a` (4.5:1+ everywhere). Also fixed the 404 page's big "404" number, which I'd initially styled yellow-on-white (1.6:1, fails badly even at the relaxed large-text 3:1 floor) - switched to navy, matching the site's other big display numbers (trust-value, about-stat-value). Re-verified the full pairing matrix after both fixes: everything real passes now.

  **Mobile breakpoint sweep (375/768/1024px)** - screenshotted and automated-overflow-checked all 14 unique page templates at all 3 widths (42 combinations, zero horizontal overflow found). Found one real, non-cosmetic bug this way: the Contact page's form grid was still rendering 2 columns at mobile widths, silently defeating the intended single-column layout, even though `contact.dart`'s own mobile media query correctly said `grid-template-columns: 1fr`. Root cause: `.cf-turnstile` (defined in the separate `contact_form.dart`) still spanned 2 grid columns at every width with no mobile override, so Chrome auto-generated an implicit second column to fit it - spec-compliant grid behavior, not a cascade bug, but easy to misread as one (an `!important` override was tried and briefly landed before the real cause was found, then reverted once the actual missing mobile override in `contact_form.dart` was identified and fixed instead). Fixed by adding the missing `.cf-turnstile` mobile override, collapsing it to span 1 alongside the rest of the form. Re-verified the disabled-button gating and honeypot were still intact after touching that file again.

  **Final token cleanup** - a full-codebase grep confirmed zero remaining references to any legacy `AppColors` alias (`primary`, `accent`, `ink`, `body`, `muted`, `lightBlueTint`, `borderLight`, `borderMedium`, `pillTint`, `shadedBg`, `placeholderBorder`, `placeholderStripe`, `warmTint`/`warmBorder`/`warmText`/`warmMutedText`, `footerNavy`) anywhere in `lib/` - every page batch had migrated its own call sites by the time it was touched. Removed the now-dead alias block from `theme.dart` entirely (kept as a compatibility shim since 11.1, no longer needed). Also caught and fixed three token references outside any specific page batch's scope: `theme.dart`'s own global styles (`html, body` text color, sitewide focus-visible outline), `photo_placeholder.dart` (shared by many pages, never "owned" by a single batch - now peach-toned dashed border/stripe, matching the pattern already used for photo-shaped content elsewhere), and `support_us.dart` + `app.dart`'s skip-link (both untouched by any prior batch, `support_us.dart` explicitly flagged in this file as "not part of any documented batch" but still a live page reachable from the About dropdown).

  **Verified:** `dart analyze` clean, static build succeeds for all 21 routes, and screenshots/functional checks confirm the fixes above actually work (not just "should work" from reading the CSS).

## Active: Step 12 - Compliance + Data Privacy and Security Pages

Two new About Us sub-pages, ported from the legacy WordPress site: `/about-us/compliance/` and `/about-us/data-privacy-and-security/`. Content sources (fetched 2026-08-13):

- https://littlevillage.org/about-us/compliance/
- https://littlevillage.org/about-us/data-privacy-and-security/

**Compliance** is plain prose - no PDFs, no embeds. It's just another `page` document, same pattern as Mission/History/Founders/Facilities. No new schema needed.

**Data Privacy and Security** is the real work: prose sections interleaved with 1 YouTube video, 5 PDF downloads, a plain-links section, and a bulleted resource list. Rather than a new document type, extend the existing shared `blockContent` array (used by every `page` already) with two new block types: `fileDownload` and `videoEmbed`. The Federal Laws links and Parent Resources list are just standard portable-text links/bullets - no schema needed for those.

**On the YouTube embed specifically:** this does NOT need a `@client` island or any JS - an `<iframe>` embed is plain HTML. Use `youtube-nocookie.com` (not `youtube.com`) as the embed domain - it's the privacy-enhanced mode (no tracking cookie set until the visitor clicks play), which is a nice fit thematically given this is literally the data-privacy page. Extract the video ID from the stored URL rather than storing a raw embed URL, so the Sanity field can hold a plain YouTube URL (editor-friendly) and the Dart component builds the iframe src.

Source PDFs to pull from the legacy site and re-upload as Sanity file assets (do not link directly to littlevillage.org - upload them as real assets in this dataset):

- Parents' Bill of Rights (English): `https://littlevillage.org/wp-content/uploads/2023/08/Parents-Bill-of-Rights.pdf`
- Parents' Bill of Rights (Spanish): `https://littlevillage.org/wp-content/uploads/2023/08/Declaracion-de-derechos-de-los-padres.pdf`
- HLVS Data Privacy and Security Standards: `https://littlevillage.org/wp-content/uploads/2023/08/HLVS-Data-Privacy-and-Security-Standards.pdf`
- NYSED Data Security and Privacy Policy: `https://littlevillage.org/wp-content/uploads/2023/07/nysed-data-privacy-and-security-policy.pdf`
- FERPA Notification: `https://littlevillage.org/wp-content/uploads/2023/07/FERPA-Notification.pdf`

Video: `https://www.youtube.com/embed/JMsd3XIHrho?feature=oembed` (video ID `JMsd3XIHrho`), caption "NYS Education Law 2-d video."

**Content note (resolved 2026-08-13, per Mike):** the Compliance page names a specific staff member (Staci Sherman, Compliance Officer) with a direct phone extension and email. Confirmed still current - port verbatim as plain inline text (name, title, phone, email), no styling changes.

- [x] **12.1 - hlvs-studio: schema** - added `fileDownload` (`title` string required, `file` file-asset required, optional `label` string override for cases like the NYSED link where the link text itself is the title rather than a generic "Download") and `videoEmbed` (`url` string required, `caption` string optional) as new object types in `blockContent.js`'s `of` array, alongside the existing `block`/`image` types. Both are defined inline in `blockContent.js` rather than as separate files, matching how `image` is already handled there. Verified with `sanity schema validate` (0 errors, 0 warnings). Committed and pushed to `hlvs-studio` `origin/main` (`69fe641`) - not left local-only, per the `parentAssociation` drift lesson from Step 10.

- [x] **12.2 - hlvs-studio: content entry** - both `page` documents created and published in the `production` dataset (slugs `compliance` and `data-privacy-and-security`), content transcribed from the live legacy pages via a raw-HTML fetch (not the summarized WebFetch pass, which had paraphrased some text and misplaced the Dignity Act/Staci Sherman paragraph into a wrong section - re-verified against actual `<p>`/`<a>` tags before writing anything). Compliance: 3 paragraphs verbatim, Staci Sherman contact info included inline exactly as on the source page (confirmed still current, per Mike). Data Privacy and Security: 30 body blocks in legacy section order - intro/NYS Ed Law 2-d paragraph, `videoEmbed` (YouTube URL stored as a plain `watch?v=` link per the doc's note, not the embed/oembed URL - the embed URL gets built in 12.3), 5 `fileDownload` blocks each referencing a real uploaded PDF asset, Federal Laws and Parent Resources as bulleted links with real hrefs (verified against source `<a href>`s, not guessed), one `label` override used on the NYSED entry only (its source link text is the title itself, no separate generic "Download" - the other 4 don't get a label). Uploaded all 5 PDFs as real Sanity file assets (not linked to littlevillage.org) via a one-off `sanity exec --with-user-token` script (the MCP tool set has no direct asset-upload tool); script deleted after use, not committed. Verified via GROQ query: both documents' block/asset structure and the video URL match spec exactly.

- [x] **12.3 - Dart: render the new block types** - `queries.dart`'s `pageBySlugQuery` body projection extended with a `_type == "fileDownload" => { "fileUrl": file.asset->url }` clause (mirrors the existing `image` clause). `portable_text_view.dart`: `fileDownload` renders via the exact existing `.link-grid`/`.link-card`/`.link-card-title`/`.link-card-cta` classes already defined in `content_page.dart` for newsletter downloads - no new CSS needed for it. Consecutive `fileDownload` blocks group into one `.link-grid` (new grouping logic in `_renderBlocks`, same pattern already used for consecutive bullet blocks) so the two Bill of Rights PDFs render as a side-by-side pair like newsletters do, not stacked. When a block's optional `label` is set (the NYSED case), the card shows only that text as its one line rather than a generic title + "Download" pairing - matches the schema field's documented intent. `videoEmbed` renders a responsive `youtube-nocookie.com` iframe using CSS's native `aspect-ratio: 16/9` (no padding-hack needed, confirmed Jaspr's `Styles` API supports `aspectRatio` directly), with `title` set to the block's `caption` (falls back to "Embedded video", never blank or bare "video"); video ID is extracted from whatever editor-friendly URL shape the field holds (`watch?v=`, `youtu.be/`, `embed/`) rather than assuming one format. No `@client` needed for either, per the plan. Verified: `dart analyze` clean project-wide, static build succeeds for all 21 existing routes (the two new pages aren't routed yet - that's 12.4, where real rendering gets checked against the actual Sanity content created in 12.2).

- [x] **12.4 - Routes + nav** - **corrected during implementation:** used flat `/compliance` and `/data-privacy-and-security` paths, not the `/about/compliance` / `/about/data-privacy-and-security` this line originally specified - every other About sub-page (Mission/History/Founders/Staff/Board/Facilities/Support Us) already uses a flat top-level path, and nesting these two under `/about/` would've been the first exception to that with no real benefit. Confirmed with Mike before building. `lib/pages/compliance.dart` and `lib/pages/data_privacy_and_security.dart` added, identical pattern to `founders.dart`/`mission.dart` (fetch via `ContentRepository.getPage()`, render through `ContentPage` + `PortableTextView`). Routes registered in `app.dart`. Both added to `header.dart`'s `aboutLinks` array and its `aliases` list (positioned after Board Members, before Support Us - matches the legacy WordPress About Us submenu order, where Support Us doesn't exist at all), and to `about.dart`'s `_subNav` row. **Verified against the actual built static output, not just source review:** `/compliance` and `/data-privacy-and-security` both generate in the static build (23 routes total, up from 21); real Sanity content confirmed in the rendered HTML (Compliance's `<h1>`, Staci Sherman's phone/email); all 5 `fileDownload` links resolve to real `cdn.sanity.io` asset URLs, not placeholders; the video iframe renders with the correct `youtube-nocookie.com` src and title; fileDownload blocks group into 4 `.link-grid`s as expected (the two Bill of Rights PDFs paired into one grid, the other three each alone); both nav locations render the new links with correct `label`/`path` data.

- [ ] **12.5 - SEO meta + verification** - apply `seo_meta.dart` to both pages (per the 7.2/7.3 convention). Verify: both pages render real Sanity content (not placeholder), all 5 PDF downloads actually download the correct file, the YouTube video renders and plays correctly including at mobile widths, nav links show correct active state, and confirm `sitemap.xml` (7.4) picks up both new routes automatically via its build-time route enumeration rather than needing a hand-maintained list.

## Deferred until launch week (mid-September 2026): Step 9d - Custom Domain Cutover

Do not start this step under any circumstances until explicitly told launch is imminent, even if 9b and 9c are both done and verified. Note: this is separate from the Resend subdomain (`send.littlevillage.org`) already set up for 9c - that's isolated and doesn't touch the site's main A/CNAME records.

- [ ] **9d.1 - Production custom domain** - add littlevillage.org as a custom domain on the Cloudflare Pages project.
- [ ] **9d.2 - DNS cutover at GoDaddy** - update DNS to point at Cloudflare.

## Open question

`pages/support_us.dart` exists (added 2026-07-28, linked from the About dropdown) but isn't part of any documented batch, and the header's Donate pill still links to `href="#"`. Needs a decision: does Support Us replace the WordPress -> Give Lively Donate link, or is it separate? Where should the Donate pill point?

## Reference: current WordPress nav (for parity-checking)

Programs and Enrollment (+ Educational Programs, Early Intervention, Preschool, Elementary, Therapeutic Services, Family Services, CPSE Evaluations, Enrollment Info, Summer Rec) - About Us (+ Mission, History, Founders, Admin Staff, Board Members, Upcoming Events, Compliance, Data Privacy & Security, Career Opportunities) - School Facilities - Media (+ Newsletters, In The News, Videos, Pictures) - Contact - Donate (external, Give Lively)
